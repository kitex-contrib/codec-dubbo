dubbo:
  consumer:
    references:
      "UserProvider":
        protocol: "dubbo"
        interface: "org.apache.dubbo.UserProviderProxy"
        url: "dubbo://127.0.0.1:20000"
  logger:
    zap-config:
      level: error