dubbo:
  protocols:
    dubbo:
      name: dubbo
      port: 20000
  provider:
    services:
      UserProvider:
        interface: org.apache.dubbo.tests.api.UserProvider  # should be consistent with java class name
